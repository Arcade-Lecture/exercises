When installing XCode for app development it added clang as compiler. Because clang and gcc are often offered as compilers to use, I didn't see any reason to install gcc too. XCode even linked the gcc command to clang...
